package components;

import org.openqa.selenium.WebDriver;
import pageobject.PageObject;

public class AbsComponent extends PageObject {
    public AbsComponent(WebDriver driver) {
        super(driver);
    }
}
